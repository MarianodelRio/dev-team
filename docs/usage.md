# Usage

<!--
  Library doc template — use this when project.type is `library`. /bootstrap generates
  the primary doc for your project type from the Documentation plan in design.md.

  Maintained by agents during development.
  Every new public function/class must be documented here in the same PR that adds it.
  Document the public API only — internal helpers do not belong here.
-->

## Install

```bash
# pip install mylib   |   npm install mylib   |   go get mylib/...
```

## Quickstart

```python
import mylib

result = mylib.parse("input")
print(result)
```

## Public API

<!--
### `mylib.parse(text: str) -> Result`

[What it does.]

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `text` | str | The input to parse |

**Returns** — `Result`: [what it contains]

**Raises** — `ValueError` when [condition]

**Example**

```python
mylib.parse("x")  # → Result(value="x")
```
-->
